
let pippo=0

while(pippo<=100)
{ 
    console.log(pippo);
    pippo=pippo + 1;
}

if(pippo%3)
{
    pippociao;
}









/*Scrivete un programma che usi console.log
per stampare tutti i numeri da 1 a 100, con due eccezioni. 
Per i numeri divisibili per 3, stampa "Fizz"invece del numero, 
e per i numeri divisibili per 5 (e non 3), stampa "Buzz"invece.

Quando hai funzionato, modifica il tuo programma per stampare "FizzBuzz"per 
numeri che sono divisibili sia per 3 che per 5 
(e continua a stampare "Fizz"o "Buzz"per numeri 
divisibili solo per uno di quelli).*/